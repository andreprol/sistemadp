@model IEnumerable<SistemaDP.Models.HistoricoSalario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.data_mod_salario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.salario_inicial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.salario_atual)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.data_mod_salario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.salario_inicial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.salario_atual)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
